# Core dependencies — expand as we build each phase
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Agent (Phase 6)
langgraph>=0.2.0
langchain-core>=0.3.0
